"""Web3 integration package.

Submodules:
    contracts/  — ABI loaders + chain-side contract wrappers (web3.py)
    services/   — read-side helpers (ownerOf, getDataset, etc.)
    listeners/  — async chain log indexer
    utils/      — hex / topic / log decode helpers
"""
